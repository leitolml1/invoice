import {} from '../../schemas/index.js';
import { handleBatchCompletionStream } from '../../server/rpc/handlers/batch-completion-stream.js';
import { handleCompletionOrchestrate } from '../../server/rpc/handlers/completion-orchestrate.js';
import { handleCompletionStream } from '../../server/rpc/handlers/completion-stream.js';
import { handleDownloadAsset } from '../../server/rpc/handlers/download-asset.js';
import { handleLoadModel } from '../../server/rpc/handlers/load-model/index.js';
import { handleLoadModelDelegated } from '../../server/rpc/handlers/load-model-delegated.js';
import { handleCompletionStreamDelegated } from '../../server/rpc/handlers/completion-stream-delegated.js';
import { getModelEntry } from '../../server/bare/registry/model-registry.js';
import { handleUnloadModel } from '../../server/rpc/handlers/unload-model.js';
import { handleUnloadModelDelegated } from '../../server/rpc/handlers/unload-model-delegated.js';
import { handleTranscribe } from '../../server/rpc/handlers/transcribe.js';
import { handleTranscribeStream } from '../../server/rpc/handlers/transcribe-stream.js';
import { handleBciTranscribe } from '../../server/rpc/handlers/bci-transcribe.js';
import { handleBciTranscribeStream } from '../../server/rpc/handlers/bci-transcribe-stream.js';
import { handleEmbed } from '../../server/rpc/handlers/embed.js';
import { handleTranslate } from '../../server/rpc/handlers/translate.js';
import { handleLoggingStream } from '../../server/rpc/handlers/logging-stream.js';
import { cancelHandler } from './handlers/cancelHandler.js';
import { provideHandler } from './handlers/provideHandler.js';
import { stopProvideHandler } from './handlers/stopProvideHandler.js';
import { handleRag } from '../../server/rpc/handlers/rag.js';
import { handleDeleteCache } from '../../server/rpc/handlers/delete-cache.js';
import { handleTextToSpeech } from '../../server/rpc/handlers/text-to-speech.js';
import { handleTextToSpeechStream } from '../../server/rpc/handlers/text-to-speech-stream.js';
import { handleGetModelInfo } from '../../server/rpc/handlers/get-model-info.js';
import { handleGetLoadedModelInfo } from '../../server/rpc/handlers/get-loaded-model-info.js';
import { handleGetSystemResources } from '../../server/rpc/handlers/get-system-resources.js';
import { handleOCRStream } from '../../server/rpc/handlers/ocr-stream.js';
import { handleHeartbeat } from '../../server/rpc/handlers/heartbeat.js';
import { handleFinetune } from '../../server/rpc/handlers/finetune.js';
import { handleHeartbeatDelegated } from '../../server/rpc/handlers/heartbeat-delegated.js';
import { handleCancelDelegated } from '../../server/rpc/handlers/cancel-delegated.js';
import { handleDiffusionStream } from '../../server/rpc/handlers/diffusion-stream.js';
import { handleVideoStream } from '../../server/rpc/handlers/video-stream.js';
import { handleUpscaleStream } from '../../server/rpc/handlers/upscale-stream.js';
import { handleClassify } from '../../server/rpc/handlers/classify.js';
import { handleAudioGenStream } from '../../server/rpc/handlers/audio-gen-stream.js';
import { handlePluginInvoke, handlePluginInvokeStream } from '../../server/rpc/handlers/plugin-invoke.js';
import { handleModelRegistryList, handleModelRegistrySearch, handleModelRegistryGetModel } from '../../server/rpc/handlers/registry.js';
import { handleSuspend } from '../../server/rpc/handlers/suspend.js';
import { handleResume } from '../../server/rpc/handlers/resume.js';
import { handleState } from '../../server/rpc/handlers/state.js';
function ragSupportsProgress(request) {
    if (request.type !== 'rag')
        return false;
    return ['ingest', 'saveEmbeddings', 'reindex'].includes(request.operation);
}
function finetuneSupportsProgress(request) {
    if (request.type !== 'finetune')
        return false;
    return ['start', 'resume', undefined].includes(request.operation);
}
function isModelDelegated(request) {
    if (!('modelId' in request))
        return false;
    const entry = getModelEntry(request.modelId);
    return entry?.isDelegated ?? false;
}
/**
 * Should the cancel be forwarded to a delegated provider?
 *
 * After the 0.11.0 wire-schema collapse the cancel envelope has two
 * operations:
 *
 *  - `request` — targeted cancel by `requestId`. Always handled
 *    locally: the worker-singleton `RequestRegistry` is the source of
 *    truth for active requests (delegated handlers register their own
 *    requests on it the same way local handlers do), so a `requestId`
 *    cancel always lands on the right worker without needing a hop
 *    through the provider. Returning `false` here keeps the cancel on
 *    the local cancel handler, where it routes through the registry
 *    and (for downloads) the `markClearCacheForRequest` helper.
 *
 *  - `broad` — abort every in-flight request on a model. Forwarded to
 *    the delegated provider iff the targeted model itself is
 *    delegated; the provider then runs the same broad-cancel sweep
 *    server-side. Local broad cancels for non-delegated models stay
 *    on this worker.
 */
function isCancelDelegated(request) {
    if (request.type !== 'cancel')
        return false;
    if (request.operation !== 'broad')
        return false;
    return isModelDelegated(request);
}
export const registry = {
    // Simple Reply handlers
    heartbeat: {
        type: 'reply',
        handler: handleHeartbeat,
        delegatedHandler: handleHeartbeatDelegated,
        isDelegated: (r) => r.type === 'heartbeat' && !!r.delegate
    },
    unloadModel: {
        type: 'reply',
        handler: handleUnloadModel,
        delegatedHandler: handleUnloadModelDelegated,
        isDelegated: isModelDelegated
    },
    embed: { type: 'reply', handler: handleEmbed },
    cancel: {
        type: 'reply',
        handler: cancelHandler,
        delegatedHandler: handleCancelDelegated,
        isDelegated: isCancelDelegated
    },
    provide: { type: 'reply', handler: provideHandler },
    stopProvide: { type: 'reply', handler: stopProvideHandler },
    deleteCache: { type: 'reply', handler: handleDeleteCache },
    getModelInfo: { type: 'reply', handler: handleGetModelInfo },
    getLoadedModelInfo: { type: 'reply', handler: handleGetLoadedModelInfo },
    getSystemResources: { type: 'reply', handler: handleGetSystemResources },
    pluginInvoke: { type: 'reply', handler: handlePluginInvoke },
    modelRegistryList: { type: 'reply', handler: handleModelRegistryList },
    modelRegistrySearch: { type: 'reply', handler: handleModelRegistrySearch },
    modelRegistryGetModel: {
        type: 'reply',
        handler: handleModelRegistryGetModel
    },
    suspend: { type: 'reply', handler: handleSuspend },
    resume: { type: 'reply', handler: handleResume },
    state: { type: 'reply', handler: handleState },
    // Simple Stream handlers
    audioGenStream: { type: 'stream', handler: handleAudioGenStream },
    transcribe: { type: 'stream', handler: handleTranscribe },
    transcribeStream: { type: 'duplex', handler: handleTranscribeStream },
    bciTranscribe: { type: 'stream', handler: handleBciTranscribe },
    bciTranscribeStream: { type: 'duplex', handler: handleBciTranscribeStream },
    loggingStream: { type: 'stream', handler: handleLoggingStream },
    translate: { type: 'stream', handler: handleTranslate },
    textToSpeech: { type: 'stream', handler: handleTextToSpeech },
    textToSpeechStream: { type: 'duplex', handler: handleTextToSpeechStream },
    ocrStream: { type: 'stream', handler: handleOCRStream },
    diffusionStream: { type: 'stream', handler: handleDiffusionStream },
    videoStream: { type: 'stream', handler: handleVideoStream },
    upscaleStream: { type: 'stream', handler: handleUpscaleStream },
    classify: { type: 'stream', handler: handleClassify },
    pluginInvokeStream: { type: 'stream', handler: handlePluginInvokeStream },
    // Handlers with delegation support
    loadModel: {
        type: 'reply',
        handler: handleLoadModel,
        delegatedHandler: handleLoadModelDelegated,
        isDelegated: (r) => r.type === 'loadModel' && !!r.delegate,
        supportsProgress: true
    },
    completionStream: {
        type: 'stream',
        handler: handleCompletionStream,
        delegatedHandler: handleCompletionStreamDelegated,
        isDelegated: isModelDelegated
    },
    // Deliberately no delegated handler: tool callbacks execute code on the
    // client machine, so only the user's own local worker may orchestrate
    // (see completionOrchestrateResponseSchema's contract note).
    completionOrchestrate: { type: 'duplex', handler: handleCompletionOrchestrate },
    batchCompletionStream: {
        type: 'stream',
        handler: handleBatchCompletionStream
    },
    // Handlers with progress support
    downloadAsset: {
        type: 'reply',
        handler: handleDownloadAsset,
        supportsProgress: true
    },
    rag: {
        type: 'reply',
        handler: handleRag,
        supportsProgress: ragSupportsProgress
    },
    finetune: {
        type: 'reply',
        handler: handleFinetune,
        supportsProgress: finetuneSupportsProgress
    }
};
