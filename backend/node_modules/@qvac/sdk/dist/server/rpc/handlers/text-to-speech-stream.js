import { dispatchPluginStream } from '../../../server/rpc/handlers/plugin-dispatch.js';
export async function* handleTextToSpeechStream(request, inputStream) {
    yield* dispatchPluginStream(request.modelId, 'textToSpeechStream', request, inputStream);
}
