import { ModelTypeAliases } from '../../schemas/model-types.js';
import { ENGINE_TO_ADDON, LEGACY_ENGINE_TO_CANONICAL } from '../../schemas/engine-addon-map.js';
function sortedEntries(record) {
    const entries = Object.entries(record);
    entries.sort(function (a, b) {
        return a[0] < b[0] ? -1 : a[0] > b[0] ? 1 : 0;
    });
    return Object.fromEntries(entries);
}
export function buildModelTypeMaps() {
    return {
        aliasToCanonical: sortedEntries(ModelTypeAliases),
        engineToAddon: sortedEntries(ENGINE_TO_ADDON),
        legacyEngineToCanonical: sortedEntries(LEGACY_ENGINE_TO_CANONICAL)
    };
}
